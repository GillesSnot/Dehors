{% extends 'base.html.twig' %}
{% block title %}Liste des sorties{% endblock %}

{% block body %}


    {# <form>
        <select name='select_campus'>
            {% for campu in campus %}
                <option></option>
            {% endfor %}
        </select>
    </form> #}
    <input type="select"></input>


    <div class="w-75 h-25 position-absolute top-50 start-50 translate-middle" style="overflow-x: hidden; overflow-y: auto; height: 100px;">
        <table class="table table-striped">
            <thead>
                <tr>
                <th scope="col">Nom de la sortie</th>
                <th scope="col">Date de la sortie</th>
                <th scope="col">Clôture</th>
                <th scope="col">Inscrits/Places</th>
                <th scope="col">Etat</th>
                <th scope="col">Inscrit</th>
                <th scope="col">Organisateur</th>
                <th scope="col">Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for sortie in sorties %}
                        <td>{{sortie.nom}}</td>
                        <td>{{sortie.dateSortie|format_datetime()}}</td>
                        <td>{{sortie.dateFinInscription|format_datetime()}}</td>
                        <td>{{sortie.nombreParticipants}}/{{sortie.nombrePlace}}</td>
                        <td>{{sortie.etat}}</td>
                        <td>jsp encore</td>
                        <td>{{sortie.organisateur.prenom}} {{sortie.organisateur.nom}}</td>
                        <td>jsp encore</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

{% endblock %}
